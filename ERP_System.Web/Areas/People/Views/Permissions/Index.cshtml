



    <div class="row page-titles mx-0">
        <div class="col-12 p-0 m-0">
            <div class="d-flex  justify-content-between align-items-center">
                <h4 class="PageTitle">@CurrentForm.Title  </h4>
                <div class="d-flex justify-content-between align-items-center gap-2">

                    <a class="SaveBtn" style="cursor:pointer" id="btnSave" onclick="SaveUserPermission()">
                        <div class="btn btn-primary">
                            حفظ
                            <i class="fas fa-save"></i>
                        </div>


                    </a>

                </div>
            </div>
        </div>
        <div class=" col-12  m-0  CardDiv my-2">
            <div class="w-100 d-flex flex-wrap align-items-end   mt-3 gap-3">


            </div>
        </div>
    </div>

    <div class="row">

        <div class="card">
        <div class="w-25">
            <div class="p-4 d-flex align-items-center" style="justify-content: space-evenly;">
                <label class="lable" for="ddlRegions">نوع المستخدم</label>
                <select onchange="ShowUserPermission()" class="Select sAddress form-select w-50" aria-label="اختر نوع المستخدم" asp-items='new SelectList((IEnumerable<SelectListDTO>)ViewData["UserTypeId"], "Value", "Text")' name="UserTypeId" id="ddlUserType">
                    <option value="@Guid.Empty">اختر نوع المستخدم</option>
                </select>

            </div>
                  
            </div>
            <div class="card-body">

                <div class="row">

                    <table id="tbl" class="table " width="100%">
                        <thead>
                            <tr class="text-center">
                                <th>#</th>
                                <th>الصفحة</th>
                                <th>اضافة</th>
                                <th>عرض</th>
                                <th>تعديل</th>
                                <th>حذف</th>
                               @* <th>مرتجع شراء</th>
                                <th>مرتجع بيع</th>*@
                                <th>تحديد / الغاء الكل</th>

                            </tr>
                        </thead>
                        <tbody style="vertical-align:middle;text-align:center;" id="tbodyUserPermissions"></tbody>
                    </table>
                </div>

            </div>
        </div>
    </div>




    @section Scripts{

        <partial name="_PermissionsScripts" />
         <script>
        $(document).ready(function () {
            $("#ddlUserType").val('5a45d22f-b9f5-4b28-be74-0f1d16ab1efa').trigger('change');
        });
    </script>
    }
